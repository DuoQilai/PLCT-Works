核心函数：
1.pinMode() - 引脚模式设置函数；
2.digitalWrite() - 数字输出函数；
3.delay() - 毫秒延时函数


题目1：初始化 LED
int main() {
    // 初始化 WiringX
    if(wiringXSetup("milkv_duos", NULL) == -1) {
        printf("WiringX 初始化失败\n");
        return -1;
    }
    
    // 初始化 LED 引脚
    pinMode(______, ______);        // 填空1：设置引脚模式
    digitalWrite(______, ______);   // 填空2：设置初始状态
    
    // 其他代码...
}


LEDPIN, PINMODE_OUTPUT

LEDPIN, LOW


题目2：发送起始信号
// 1. 发送起始信号
pinMode(DHTPIN, ______);           // 填空3：设为输出模式
digitalWrite(DHTPIN, ______);      // 填空4：拉高电平
delay(500);
digitalWrite(DHTPIN, ______);      // 填空5：拉低电平
delay(20);
pinMode(DHTPIN, ______);           // 填空6：设为输入模式


PINMODE_OUTPUT

HIGH

LOW

PINMODE_INPUT


题目3：判断温度控制LED
if(read_dht22(&temp, &humi)) {
    // 控制 LED
    if(______) {                    // 填空7：温度大于30度的条件
        digitalWrite(LEDPIN, ______);  // 填空8：点亮LED
        printf("温度:%.1f°C → LED 亮\n", temp);
    } else {
        digitalWrite(LEDPIN, ______);  // 填空9：熄灭LED
        printf("温度:%.1f°C → LED 灭\n", temp);
    }
}


temp > 30.0

HIGH

LOW


题目4：等待间隔
// DHT22 采样周期至少2秒
______(2000);           // 填空12：等待2秒的函数


delay